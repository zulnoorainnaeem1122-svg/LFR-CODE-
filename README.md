the project is a line following robot built using sensors and a microcontroller the robot automatically follows a black line on a white surface using ir sensor and motor control logic 
components used 
ardiuno uno, ir sensor array, motor driver , dc motors, chassis, battery pack, wires and bread board 
working principle
the robot uses ir sensor to detect the line:
if both sensor detect line turn left
